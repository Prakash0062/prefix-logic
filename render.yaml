services:
  - type: web
    name: braille-ocr-app
    env: python
    plan: free
    buildCommand: |
      pip install -r requirements.txt
      ./build.sh
    startCommand: python app.py
    envVars:
      - key: FLASK_ENV
        value: production
